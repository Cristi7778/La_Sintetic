import {SafeAreaView, Text, View } from 'react-native';

export default function AddReservations() {
  return (
    <SafeAreaView >
      <Text>Add Reservations Screen</Text>
    </SafeAreaView>
  );
}